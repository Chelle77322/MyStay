//import {LOGIN_SUCCESS, LOGIN_FAIL, LOGOUT} from '../actions/types';

//const INITIAL_STATE = {
  //  isAuth: false,
    //errors: [],
    //booking_id: ''
//}
//export const authReducer = (state = //INITIAL_STATE, action) => {
//    switch(action.type){
        
  //      case LOGIN_SUCCESS:
    //        return Object.assign({}, state, {isAuth:true, errors:[], booking_id: action.booking_id});

//            case LOGIN_FAIL:
  //              return Object.assign({}, state, {errors: action.errors});

    //        case LOGOUT:
      //          return Object.assign({}, state, {isAuth: false});
        //        default:
          //          return state;
   // }
//}